---
description: "Harness Engineering v3 core constraints - always on"
alwaysApply: true
---

# Harness Engineering Core

- This project follows the Harness Engineering v3 framework.
- Persistent artifacts: `research.md`, `plan.md`, `progress.md`, `test.md`, plus sprint `contract.md`.
- Artifact budgets: `research.md` <= 40% context; other artifacts <= 20%.
- The Generator never evaluates its own work.
- Failed approaches MUST be read before implementation begins.
- Spike validation is required for uncertain integrations.
- Read `.cursor/state/active-phase` to confirm current phase before tool use.
