# /evaluate

Run the Evaluator Agent (Phase 3):

1. Use the `@evaluator` skill, Mode B (Build Evaluation).
2. Do NOT read `implementation/progress.md` or implementation reasoning.
3. Read only: `planning/contract.md` and the deployed build.
4. Execute all tests in `evaluation/test.md`.
5. Grade against `planning/contract.md` rubric.
6. Output Pass with scores OR Fail with bug reports.

Bias toward failure: surface polish is not Pass. Verify core functionality works end-to-end.
