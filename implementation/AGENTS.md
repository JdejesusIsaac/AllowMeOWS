# Implementation Phase (Phase 2)

- FIRST ACTION: Read "Failed Approaches" in `implementation/progress.md`.
- Build per `planning/plan.md` AND `planning/contract.md`.
- Deviate only on blockers; document those blockers in `implementation/progress.md`.
- Update `implementation/progress.md` on every significant change.
- Failed Approaches section: max 10 lines, persists across resets.
- Do NOT self-evaluate. Run smoke tests against contract verification criteria, then hand off.

## Available MCPs

- supabase: Database operations, schema management using Management Token, not anon JWT
- figma: Design specs and component reference
- flowglad: Payment/billing integration
- desktop-commander: Terminal automation
- upstash: Redis caching and queue operations
- github: PR workflows, issue tracking
